# MiniOneRec-Enhanced 依赖锁（2026-09-09 对齐实际训练环境 recenv）
# 安装/版本核对入口：bash setup_env.sh；实测版本见 docs/environment.txt。
# RTX 5090（sm_120）需 torch>=2.6；torch 由官方 cu130 索引安装：

--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.12.1+cu130

# 训练/推理核心
transformers==4.57.3      # 勿升 5.x：依赖 generate(use_model_defaults=...) 等行为
trl==1.12.0
accelerate==1.14.0
deepspeed==0.19.6
datasets==5.0.1
tokenizers==0.22.2

# 数据处理 / 基础库
numpy==2.4.6
pandas==3.0.5
scikit-learn==1.9.0
safetensors==0.8.0
fire==0.7.1

# 实验记录（可选）
wandb==0.29.0
